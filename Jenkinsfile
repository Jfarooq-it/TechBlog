pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
            }
        }   
         stage('Build') {
            steps {
                echo 'Testing second stage!'
            }
        }   
      }
}
